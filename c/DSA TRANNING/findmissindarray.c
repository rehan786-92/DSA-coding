#include<stdio.h>
#include<string.h>
int main(){
    // int n,m;
    // printf("Enter the size of array");
    // scanf("%d %d",&n,&m);
    int n;
    int a[n];
    
    printf("enter the integer in arrays");
    for(int i=0;i<n-1;i++){
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n-1;i++){
        for(int j=1;j<=n;j++){
            if(j!=a[i]){

            }
            
        }
    }